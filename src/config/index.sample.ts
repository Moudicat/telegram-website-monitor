const config: IConfig = {
  telegram: {
    token: '',
    userIds: [],
    groupIds: [],
    channelIds: [],
    notifyGroup: ['channel'] // channel user group
  },
  default: {
    checkInterval: 20
  }
};

export default config;